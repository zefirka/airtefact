doctype html
block vars
  - var language = locals.language;

block root
  html(lang=language)
    block head
      head
        include ./head.jade
    body
      .g-overlays
        block overlays
      .g-content
        block content
      .g-footer
        block footer
      block scripts 
        p1 сюда бы перетащить скрипты, а то они пока в хедере 
        script(type='text/javascript').
            var socket = io();
            socket.on('getElement', function(msg){
                $('.g-content').append(msg);
            });